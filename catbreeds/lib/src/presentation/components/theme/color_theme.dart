import 'package:flutter/material.dart';

final shadowColor = Colors.black.withAlpha(39);

const cbColorScheme = ColorScheme(
  // Light or Dark?
  brightness: Brightness.light,
  // Primary
  primary: primary,
  onPrimary: Colors.white,
  primaryContainer: colorMarca1Variante,
  onPrimaryContainer: Colors.white,
  // Secondary
  secondary: secondary,
  onSecondary: colorNull,
  secondaryContainer: colorContinuar,
  onSecondaryContainer: colorNull,
  // Tertiary
  tertiary: colorNull,
  onTertiary: colorNull,
  tertiaryContainer: colorNull,
  onTertiaryContainer: colorNull,
  // Error
  error: colorError,
  onError: colorNull,
  errorContainer: colorNull,
  onErrorContainer: colorNull,
  // Surface
  surface: primary,
  onSurface: colorTextoPrincipal,
  // Other
  outline: colorNull,
  shadow: colorNull,
  inverseSurface: colorDisableButton,
  onInverseSurface: colorNull,
  inversePrimary: colorNull,
);

/// Figma
const primary = Color(0xFFDC761C);
const colorMarca1Variante = Color(0xFF5F259F);
const colorGradient = Color(0xFFC86DD7);
const colorMarca2 = Color(0xFF00BFB3);
const colorBienestar = Color(0xFF76DFC1);
const colorAlerta = Color(0xFFF7F777);
const colorError = Color(0xFFDB1212);
const colorContinuar = Color(0xFF00BFB3);
const colorInputsInactivos = Color(0xFF979797);
const secondary = Color(0xFFF5F5F5);
const colorFondoCards = Color(0xFFFFFFFF);
const colorFondoCardsSeleccionadas = Color(0xFFFAF2FF);
const colorTextoPrincipal = Color(0xFF343434);
const colorTextoSecundario = Color(0xFF9B9B9B);
const colorLineas = Color(0xFFD8D8D8);
const colorIconosInactivos = Color(0xFF606060);
const colorCroutonError = Color(0xFFFF4D75);
const colorCroutonInfo = Color(0xFF00BFB3);
const colorDisableButton = Color(0xFFE0E0E0);
const colorDivider = Color(0xFFADACAD);

/// Hdeleon
const colorSombra = Colors.black;
const colorNull = Colors.brown;
